- **Perceive8 adapter: real-time streaming frame sink + directed mic toggle (#1).** Adds a streaming frame sink that feeds captured audio to an external Perceive8 endpoint and a directed-mic toggle for targeted input. Merged `upstream/main` locally and resolved the `capture-bridge.ts` teardown conflict (kept both the new `frameSink.stop()` cleanup and the upstream `countersTimer` cleanup). Currently lives on the fork only; upstream contribution is deferred until the integration has a live acceptance run and a non-author reviewer.
